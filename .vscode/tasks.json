{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run Streamlit app",
			"type": "shell",
			"command": "powershell.exe",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"python -m pip install -r frontend/requirements.txt ; streamlit run frontend/streamlit_app.py --server.headless true --server.port 8555"
			]
		}
	]
}